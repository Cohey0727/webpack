## npm

```shell script
# show package info
$ npm info {package}

# install package
$ npm install --save {package}@{version}

# install package only use when developing
$ npm install --save-dev {package}@{version}
```
